"use strict";
/* class Person1 {
  public name: string = "Mark"; // 초기값 설정해주거나 constructor 사용해서 값 설정 해주거나 둘 중 하나 해야 함
  private _age!: number; // ! 쓸 때 주의! p1.age에서 number 못나타냄

  public constructor(age?: number) { // 초기값 설정해주거나 constructor 사용해서 값 설정 해주거나 둘 중 하나 해야 함
    if(age === undefined){
      this.age = 20;
    } else {
    this.age = age;
    }
  }
  public async init() {}
}

const p3:Person = new Person(35);
// const p2 = new Person();
console.log(p3);
// p1.age = 35;
// console.log(p1.age);
// console.log(p2);
// console.log(p2.age);
*/ 
